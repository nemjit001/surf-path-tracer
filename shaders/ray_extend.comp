#version 450
#pragma shader_stage(compute)

#include "wavefront_common.glsl"

shared int processedCount;

layout(set = 1, binding = 0) buffer RayBuffer
{
	int count;
	Ray rays[];
} rayBuffer;

layout(local_size_x = 32, local_size_y = 32) in;

void main()
{
	//
}
